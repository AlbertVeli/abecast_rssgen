# If the ssh-server runs on a non-default port, for instance 6666
# then add --rsh="ssh -p 6666" at the end of the line below
RSYNC = rsync -vaz

# Change to your URL here
URL = example.com:/var/www/podcast/
